apiVersion: v1
kind: Service
metadata:
  name: flowforge-fte-service
  namespace: flowforge-fte
spec:
  selector:
    app: flowforge-fte
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
